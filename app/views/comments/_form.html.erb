<%= form_for([@post, @post.comments.build]) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
	<%= f.text_area :content, size: "24x4" %>
	<%= f.submit "Comment", :class => 'btn btn-default' %>
<% end %>