<%= form_for([@post, @post.comments.build]) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
	<%= f.text_area :content, placeholder: "Your comment here...", size: "24x4", class: "form-control" %>
	<%= f.submit "Post a reply", :class => 'btn btn-default' %>
<% end %>