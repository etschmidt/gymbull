<%= form_for(@post, :url => gympost_path,
						html: { multipart: true }) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <div class="post-form">
      <%= f.hidden_field :post_type, :value => "post" %>
      <%= f.text_area :content, placeholder: "Start typin'..." %>
      <%= f.text_field :tag_list, placeholder: "tags: eg. lifting, getbig" %>
      <span class="picture">
      <%= f.file_field :picture, accept: 'image/jpeg,image/gif,image/png' %>
      </span>
    </div>
<%= f.submit "Post from Gym", class: "btn btn-primary" %>
<% end %>