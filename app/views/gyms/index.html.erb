<% provide(:title, 'All Gyms') %>
<aside class="col-md-3">
	<section class="search">
		<%= render 'search_gym' %>
		
	</section>
</aside>

<div class="col-md-6">
	<nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand">Gyms (<%= @gyms.count %>)</a>
      </div>
    </div>
  </nav>
	
	<ul class="users">
	  <%= render @gyms %>
	</ul>
</div>

<div class="col-md-1">
	<%= link_to "Create a Gym", new_gym_path, class: "btn btn-primary" %>
</div>