<% provide(:title, @title) %>

<div class="container">
	<div class="row">
	  <div class="col-md-12">
	  	
		  <h1>Recent Activity</h1>
		
			<% @activities.each do |activity| %>
				<div class="activities">
					<%= render_activity activity %>
				</div>
			<% end %>
	
		</div>
	</div>
</div>