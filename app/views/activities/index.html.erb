<h1>Activities</h1>

<%= @activities.each do |activity| %>
<!-- this is different than rendering other lists because there are different templates
for each of the types of scitvities -->

  <div class="activity">
    <%= link_to activity.owner.name, activity.owner if activity.owner %>
    <%= render_activity activity %>
  </div>
  
<% end %>