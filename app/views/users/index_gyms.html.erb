<% provide(:title, 'All gyms') %>
<aside class="col-md-3">
	<section class="search">
		<%= render 'search_gym' %>
	</section>
</aside>

<div class="col-md-6">
	<nav class="navbar navbar-default">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand">Gyms (<%= @users.count %>)</a>
      </div>
    </div>
  </nav>
	
	<ul class="users">
	  <%= render @users %>
	</ul>
	
	<%= will_paginate %>
	
</div>